[project]
name = "occlusion-robust-gait-demo"
version = "0.1.0"
description = "Vercel deployment surface for the occlusion-robust gait recognition demo"
requires-python = ">=3.12"
dependencies = [
  "Flask>=3.0.0",
  "numpy>=1.24.0",
  "Pillow>=9.0.0"
]

[project.scripts]
app = "backend.server:app"
